token: ""
prefix: "sr!"
dbl: null # Grab DBL token here https://discordbots.org/api/docs#mybots
owners:
  - "User ID"
database:
  type: sqlite # sqlite, mysql
  name:
  user:
  pass:
battlepass:
  currentSeason: 1
  seasonEndsAt: 1567522800000 # Timestamp, in moment:
                              # moment().add(90, 'days').set({ 'hour': 0, 'minute': 0, 'second': 0, 'millisecond': 0 }).toDate().getTime()
